<Window x:Class="NewsBlog.Desktop.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NewsBlog.Desktop.ViewModel"
        xmlns:local="clr-namespace:NewsBlog.Desktop.View"
        mc:Ignorable="d"
        Title="NewsBlog" Height="350" Width="525" MinWidth="500" MinHeight="300">
    <Grid>
        <DataGrid x:Name="articles" Height="300" ItemsSource="{Binding Articles}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Cím" Binding="{Binding Title}" />
                <DataGridTextColumn Header="Író" Binding="{Binding Author}"  />
                <DataGridTextColumn Header="Dátum" Binding="{Binding Date}"  />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
